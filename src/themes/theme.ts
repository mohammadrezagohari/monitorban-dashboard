// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#6200ee'
        },
        secondary: {
            main: '#dc004e',
        },
    },
});

export default theme;
