import { Box, styled } from "@mui/material";

export const StyledUserCard = styled(Box)(() => ({
    backgroundColor: "#373040",
    borderRadius: "25px",
    padding: "12px",
    display: "flex",
    alignItems: "center",
    gap: "4px",
}));
