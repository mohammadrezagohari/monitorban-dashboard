import { styled } from "@mui/material";

export const StyledSensorCard = styled("div")(() => ({
    backgroundColor: "#373040",
    borderRadius: "25px",
    padding: "12px",
    maxWidth: "250px",
    display: "flex",
    alignItems: "center",
    gap: "4px",
}))